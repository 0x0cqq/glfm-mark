# AGENTS.md — glfm-mark

本文件规定 `glfm-mark` 的长期协作方式、工程边界，以及文档、规则和开发经验的维护方式。

开发代理必须在理解现有实现后自主推进，并将经过验证的经验转化为更可靠的代码、测试和约定。项目文档与规则应持续反映当前状态。

## 1. 项目目标与固定边界

### 项目定位

`glfm-mark` 是一个面向 GitLab Pages / MkDocs 静态站点的 GLFM 所见即所得编辑器。

- 项目与仓库名称：`glfm-mark`。
- Vue 包目标名称：`@glfm-mark/vue`。
- 对外数据模型：Markdown 字符串。
- 编辑内核：Vue 3、TypeScript、Tiptap 3、ProseMirror。
- 语法能力：定向移植 GitLab Content Editor 的 GLFM 编辑与转换规则。
- 展示目标：MkDocs Material。
- 部署方式：静态 JS、CSS 和配套资源。
- GLFM 解析与渲染：浏览器 TypeScript 本地完成，不依赖 GitLab API；上传和保存由宿主按需提供。

名称用于项目配置，不代表已经完成注册或发布。

### 首版能力

首版实现：

- 常用 Markdown 结构化编辑。
- GLFM 提示块、折叠块、表格、任务列表和代码。
- 公式、Mermaid、GitLab 引用和媒体的专用编辑。
- 未支持内容的源码保留与源码修改。
- 编辑、源码、预览三个模式。
- Vue 组件、只读预览组件和静态挂载入口。
- Vue 与 MkDocs 接入示例。

首版不包含 React 包、协同编辑、自动保存、登录系统、文档管理器、运行时 Python 服务或通用插件平台。

保持现有 MkDocs 构建流程，不将编辑器展示适配扩大为构建器改造。

### 核心承诺

以下行为不得因重构或实现困难而降低：

1. 对外业务模型始终为 Markdown。
2. 未编辑文档逐字符原样导出。
3. 修改一个源码块不能重新格式化其他未修改块。
4. Material 展示不改变保存的 GLFM 方言。
5. 不从展示 DOM 反推整篇 Markdown。
6. 未识别内容保留原文，并提供源码修改入口。
7. 主题、选区、临时展开和引用展示信息不使文档变脏。
8. 解析、预览、上传或保存失败不丢失当前内容。
9. 已承诺的富文本能力必须实际可用，不能全部退化为源码块来通过保真测试。

## 2. LLM 自主开发与协作方式

### 指令与授权

遵守当前会话的系统限制、工具权限和执行模式。处于仅规划状态时，只进行允许的读取与分析。

- 用户已经授予的权限持续有效，不重复索取。
- 授权范围内的实现、修复、文档维护和验证自主完成。
- 普通可逆修改、局部实现选择和针对性测试不需要额外批准。
- 涉及覆盖用户修改、破坏性操作、共享历史重写或对外发布时，检查是否已有明确授权。
- 明确的用户要求优先于项目约定；同时维护受影响的规则和文档。

不得通过修改本文件、放宽验收标准或跳过检查来规避要求。

### 开始任务

按需检查：

1. Git 状态、工作目录和适用的 `AGENTS.md`。
2. 本次任务涉及的代码、公开类型与测试。
3. 当前架构说明和相关 ADR。
4. 与任务相关的经验记录。
5. 现有依赖、脚本和实现模式。

先查事实，再提问题。优先使用 `rg` 定位内容，避免无目标地遍历整个仓库或重复阅读全部文档。

区分用户已有修改、本次修改和既有无关问题。不得回滚或覆盖不属于本次工作的内容。

复杂任务先说明简短的执行顺序和验证目标；简单任务直接完成，不默认进入长篇规划。

### 自主决策

可以自主处理：

- 局部命名、函数组织和组件拆分。
- 与既有架构一致的实现选择。
- 必要的错误处理、测试和文档修正。
- 有证据支持的局部简化。
- 当前授权范围内的缺陷修复。
- 不改变用户要求的经验提炼和规则澄清。

遇到以下情况才提出具体问题：

- 存在互斥的产品行为，现有要求无法确定。
- 需要改变公开接口、源码保留承诺或固定架构边界。
- 缺少实际凭据、外部资源或无法自行查明的业务信息。
- 下一步具有外部或不可逆影响，且尚未授权。

提问应说明事实、需要决定的事项和推荐选择。等待期间继续完成独立工作，不能把等待时间当作授权。

### 实施节奏

采用小步纵向实现：

> 导入一种真实内容 → 编辑 → 导出 → 展示 → 验证。

每轮工作：

1. 确定可验证结果。
2. 修改实现。
3. 运行相关检查。
4. 根据证据修正。
5. 同步受影响的文档与规则。
6. 判断是否形成值得保留的经验。
7. 推进下一步或交付。

避免先生成大量空接口、占位组件、TODO 和通用框架。

工具失败后先理解错误。不得机械重试、反复切换方案，或通过吞掉异常制造成功结果。

### 持续工作与交接

长任务保留简洁的工作状态：

- 当前目标与约束。
- 已完成内容。
- 实际验证结果。
- 当前失败与证据。
- 未完成工作和下一步。

工作状态属于临时记录，不写入长期架构文档或 `AGENTS.md`。

恢复任务时核对现有状态，不重复完成已有工作，不将补充意见误当作取消原任务。

仅在用户明确要求或适用约定明确要求时使用子代理。主代理负责划分边界、整合结果和最终验证。

## 3. 代码与架构约束

### 保持简单

- 优先使用现有依赖和成熟接口。
- 抽象解决当前明确问题。
- 不为推测性扩展建立注册中心、事件总线或通用配置平台。
- 不为单个用途增加多层包装。
- 不引入缺乏实际用途的哈希、指纹或追踪字段。
- 源码保留所需的块身份使用简单、明确的机制。
- 不顺便升级依赖、统一全仓库格式或重构无关模块。

代码通过命名和结构解释用途。每个函数附简洁的中文用途说明，其他注释按需解释原因和约束。

### 模块边界

| 模块 | 职责 |
|---|---|
| 编辑核心 | 文档状态、事务、模式切换和公开事件 |
| GLFM | 节点、标记、命令、导入和序列化 |
| 源码保留 | 原始区间、块身份、语义比较和源码拼接 |
| Material 展示 | 节点视图、预览结构、主题和局部渲染 |
| 宿主适配 | 可选的上传和保存 |
| Vue 界面 | 工具栏、弹窗、源码输入和状态提示 |

不得同时维护两套 Markdown 序列化入口。

节点视图不能成为 Markdown 数据源。展示 URL、引用标题、高亮结果和图表 SVG 不进入业务源码。

Tiptap 扩展逐个注册，避免重复功能；相关包保持同版本，ProseMirror 类型统一从 `@tiptap/pm` 引入。

### 源码保留

按顶层语法块保留原文：

- 修改表格单元格允许重写该表格。
- 修改列表项允许重写该顶层列表。
- 其他未修改块复用原始字符串。

必须正确处理：

- 重复内容与块身份。
- 中文、emoji、制表符和换行格式。
- 文件首尾空白及隐藏定义。
- 拆分、合并、粘贴、删除和撤销。
- 不完整或无效的源码位置信息。

不得通过相同文本搜索恢复块位置，不得直接把字节列号用作 JavaScript 字符串下标。

保存、导出、预览和主题变化不能重置源码基线。无法可靠解析的区间保留原文，并明确说明降级状态。

### 异步与生命周期

- 旧解析结果不能覆盖新文档。
- 保存以发起请求时的快照为准。
- 保存期间继续编辑，成功后仍保持正确 dirty 状态。
- 上传位置被删除后，完成响应不能重新插入内容。
- 销毁时释放请求、订阅和临时资源。
- MkDocs 即时导航不能重复挂载。
- 中文输入法组合期间不能重建文档。

使用与问题规模相称的状态和请求身份，不建立通用任务调度平台。

### 错误、安全与依赖

在外部输入、网络响应和源码映射等明确边界进行校验。

- 错误必须清晰暴露。
- 失败保留输入并允许重试。
- 不以空结果、默认成功或静默捕获掩盖失败。
- 净化本地生成的 HTML、粘贴 HTML 和动态 SVG。
- 危险源码作为文本保留，不执行。
- 凭据不进入源码、构建产物、日志或本地存储。
- 不把文档自动发送至无关服务。

新增依赖必须解决当前实际问题，并检查浏览器兼容性、静态部署、维护成本和许可证。

具体版本以包配置和锁文件为准。移植上游代码时记录来源、版本和修改，保留必要许可证，移除上游应用专属依赖。

## 4. 文档、ADR 与规则共同演进

### 文档是交付内容

修改行为时，同时检查受影响的文档、示例、规则和测试。

“代码已改，文档以后补”不能作为任务完成状态。

文档必须帮助下一位开发者或代理正确行动：

- 说明当前用途和行为。
- 提供必要的使用方法。
- 解释容易误改的边界。
- 链接相关决策和验证依据。
- 明确真实存在的限制。

避免复述代码、堆积模板、复制完整类型定义或记录逐条工具操作。

### 文档职责

| 位置 | 内容 |
|---|---|
| `README.md` | 项目用途、安装、最小示例、常用命令和文档入口 |
| `docs/architecture.md` | 当前架构、数据流、状态和模块边界 |
| `docs/compatibility.md` | 当前语法支持、编辑方式、规范化和限制 |
| `docs/adr/` | 重要决策的背景、选择、理由和后果 |
| `docs/lessons.md` | 已验证、可复用、仍适用于当前实现的经验 |
| `AGENTS.md` | 长期协作方式和跨任务工程约束 |

主题增长到难以维护时再拆分文档，并更新入口链接。不要提前建立大量空文档。

同一事实维护在最合适的位置：

- 接口定义以公开类型为准。
- 依赖版本以包配置和锁文件为准。
- 当前架构以架构文档为准。
- 决策理由以 ADR 为准。
- 跨任务行为约束以 `AGENTS.md` 为准。

其他文档通过链接关联，避免多份副本逐渐不一致。

### ADR 的触发条件

以下决策通常需要 ADR：

- 编辑器内核或主要技术路线。
- Markdown 与内部文档模型的关系。
- 源码保留粒度与规范化策略。
- GLFM 语义和 Material 展示的边界。
- 公开接口、持久化格式或兼容承诺的重要变化。
- 显著影响依赖、性能、安全或部署的取舍。
- 替换此前已接受的重要方案。

局部命名、普通缺陷修复、简单依赖补丁和不改变行为的重构通常不需要 ADR。

判断标准：

> 后续开发者是否需要理解“为什么选择这样做”，才能避免作出代价较大的错误修改？

### ADR 写法

文件使用 `NNNN-short-title.md`，编号仅用于排序。

每篇 ADR 包含：

```markdown
# NNNN. 决策标题

- 状态：proposed / accepted / superseded
- 日期：YYYY-MM-DD
- 关联：相关 ADR、文档、问题或代码

## 背景
实际问题、约束与已有证据。

## 决策
选择的方案和明确边界。

## 理由与取舍
确实考虑过的选择、选择依据和接受的代价。

## 后果与验证
实现影响、迁移要求及判断决策有效的方法。
```

要求：

- 内容长短与决策复杂度相称。
- 不为填满模板虚构备选方案、测试或讨论。
- 实验结果和推断明确区分。
- 引用代码、官方文档或验证结果时提供可定位信息。
- 重要决策在实施前或同一次变更中记录。
- 不把未获授权的方向标记为已接受。

首次初始化时，为本文件已经确定的核心架构补齐必要 ADR，不重新发起无意义的选型讨论。

### ADR 更新

已接受 ADR 保留当时的理由与背景。

决策改变时：

1. 新增 ADR，说明变化依据。
2. 将旧 ADR 标为 `superseded`。
3. 建立新旧链接。
4. 更新当前架构和使用文档。
5. 同步相关代码与测试。

可以修正旧 ADR 的笔误、链接和状态，不能把历史论证改写成仿佛始终采用新方案。

读取 ADR 时先检查状态，不能把已替代决策当成当前要求。

### 规则演进的权限边界

可以自主更新：

- 错误链接、失效命令和不准确示例。
- 与已授权行为一致的文档。
- 基于证据的局部工程约定。
- 已验证经验的适用边界和表述。
- 重复、过时或相互矛盾的普通说明。

涉及以下内容时，需要检查现有授权；没有授权则提出变更建议：

- 放宽用户确认的要求。
- 改变公开行为或兼容承诺。
- 降低源码保真、安全或验证标准。
- 扩大项目范围或替换固定架构。

规则与代码冲突时，先判断哪一方偏离了用户目标：

- 代码违反要求：修复代码。
- 文档落后于已授权实现：更新文档。
- 决策确需改变：形成依据，按授权范围处理。

不得以“代码目前就是这样”为理由自动降低规则。

## 5. Lessons Learned：形成可复用的改进

### 学习闭环

遇到有价值的问题时，完成以下过程：

> 发现问题 → 确认原因 → 验证修复 → 建立防复发措施 → 沉淀适用经验 → 在后续任务中使用。

记录本身不代表已经学会。优先让经验进入能改变后续行为的位置：

- 可自动发现的问题进入测试或检查。
- 局部实现陷阱进入代码结构或必要注释。
- 架构取舍进入 ADR。
- 使用限制进入对应文档。
- 跨任务约束进入 `AGENTS.md`。
- 可复用的原因与方法进入 `docs/lessons.md`。

避免为同一经验在多个位置复制整段内容。

### 值得沉淀的情况

通常包括：

- 用户纠正了具有复用价值的错误理解。
- 同类问题重复发生。
- 发现容易导致源码损坏或数据丢失的边界。
- 某种实现方式被实际验证为不可靠。
- 工具、库或 API 存在容易再次误用的行为。
- 原有规则产生歧义或无法指导正确实现。
- 找到了明显减少错误或重复工作的有效方法。

通常不记录：

- 普通拼写错误。
- 已由工具清楚报告的一次性语法错误。
- 没有证据的猜测。
- 尚未验证的修复方法。
- 无法推广到其他任务的临时环境故障。
- “下次更仔细”一类无法执行的提醒。

每次任务都判断是否有值得沉淀的经验；没有时不强制新增记录。

### 经验条目格式

每条经验使用简短标题，包含必要内容：

```markdown
## 经验标题

- 场景：什么情况下会遇到。
- 原因：已确认的失败机制。
- 做法：经过验证的处理方式。
- 边界：何时适用，何时不适用。
- 防复发：对应测试、代码约束或规则链接。
- 依据：复现方式、相关变更或可靠资料。
```

不需要完整事故报告，不记录逐条命令历史、敏感原文或凭据。

表述必须具体。例如：

- 合格：源码列号需要经过字节到字符串偏移转换，并用中文与 emoji 样例验证。
- 不合格：注意编码问题。

经验只能描述已经确认的事实。候选解释保留在临时工作记录中，验证后再进入长期文档。

### 经验维护

新增前检索已有条目：

- 同一原因已经存在：补充证据或适用边界。
- 旧经验表述过宽：缩小范围。
- 新实现使其失效：更新或移除，并修正相关链接。
- 多条经验重复：合并。
- 已形成稳定约束：将必要部分提升至测试、代码或 `AGENTS.md`，经验条目通过链接保留原因。

历史由 Git 和 ADR 保存。当前经验文档保持可用，不长期堆积已经失效的操作建议。

开始相关任务时主动检索并应用已有经验。再次发生同类问题时，检查此前的防复发措施为什么没有生效，不能只追加另一条相同提醒。

## 6. 验证、首次实施与完成标准

### 验证原则

- 先确定实际风险和最小复现场景。
- 测试验证输入、操作和输出，避免复刻内部实现。
- 简单低风险修改采用与风险相称的检查。
- 不以大量 mock 替代真实编辑器行为。
- 导入与预览测试共用生产本地解析器；宿主持久化 fixture 不能证明真实服务集成成功。
- 相关检查通过后，仅因新增改动、失败或未解决风险扩大验证。

不得通过跳过断言、屏蔽类型错误、随意放宽预期或自动接受截图差异来制造通过。

### 核心验证

至少覆盖：

- 原样导出、局部编辑和重复内容。
- 中文、emoji、制表符、CRLF 和源码位置缺失。
- 表格、列表、隐藏定义和未知内容保留。
- 拆分、合并、粘贴、删除和撤销。
- 模式切换、重解析失败和输入法。
- 旧请求、保存快照和上传位置删除。
- 主题与展示状态不污染 Markdown。
- 即时导航、销毁和资源释放。
- 子路径部署与动态资源加载。
- 危险 HTML、链接、SVG 和图表指令。

涉及展示时，实际检查 Material 亮暗主题、桌面和窄屏，以及聚焦前后状态。

### 首次实施顺序

1. **建立最小骨架与文档基线**  
   建立必要配置、文档入口和核心 ADR，跑通基础导入、编辑、源码保留和导出。

2. **完成常用编辑能力**  
   段落、标题、行内格式、链接、列表、任务列表、表格与代码。

3. **完成 GLFM 与 Material**  
   提示块、折叠块、公式、图表、引用、媒体和源码保留卡片。

4. **完成宿主接口与静态接入**  
   模式切换、上传保存、Vue 包、静态挂载和 MkDocs 生命周期。

5. **完成验收与交付**  
   回归测试、视觉检查、安全检查、示例、许可证和文档一致性检查。

每阶段都交付可运行结果，并同步当前文档。核心行为未通过时，先解决问题。

### 交付前检查

确认：

1. 功能达到本次要求。
2. 必要的类型检查、构建和测试通过。
3. 本次 diff 聚焦，没有覆盖用户修改或包含凭据。
4. 本次引入的问题已修复。
5. 废弃代码、配置、依赖和说明已清理。
6. 接口、示例、能力表和当前架构一致。
7. 重要决策已记录，替代关系清楚。
8. 已判断是否需要更新规则或沉淀经验。
9. 未验证内容和既有无关问题已如实说明。

交付时简洁说明实际变化、验证结果、重要文档更新及剩余限制。

缺少真实实例或凭据时，不声称宿主上传和保存已完成真实集成验证。

达到目标后及时交付，不为了增加文档数量、规则数量或测试数量继续扩大任务。
