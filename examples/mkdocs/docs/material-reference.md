# glfm-mark Material 参考页

本页用于与 `glfm-mark` 编辑器预览做视觉对比。内容使用 MkDocs Material 原生语法，
与编辑器中的 GLFM 语法一一对应：

| GLFM 写法 | Material 写法 |
|---|---|
| `> [!NOTE]` | `!!! note` |
| `<details><summary>` | `??? note` |
| ```` ```mermaid ```` | ```` ```mermaid ```` |
| `$`…`$` 与 ```` ```math ```` | `$…$` 与 `$$…$$` |

## 正文与行内标记

这是一个段落，包含 **粗体**、*斜体*、~~删除线~~、`行内代码` 与 [链接](https://example.com)。

## 列表

- 无序项目一
- 无序项目二
    - 嵌套项目

1. 有序项目一
2. 有序项目二

- [x] 已完成任务
- [ ] 未完成任务

## 提示块

!!! note
    这是提示正文。

!!! warning "数据删除"
    此操作不可恢复。

## 表格

| 左对齐 | 居中 | 右对齐 |
| :--- | :---: | ---: |
| a | b | c |
| 中文 | 😀 | 制表 |

## 代码

```js
const a = 1;
console.log(a);
```

## 公式

行内公式 $a^2+b^2=c^2$ 与块级公式：

$$
\int_0^1 x^2 dx
$$

## 折叠块

??? note "点击展开"
    隐藏的内容。

## 引用

> 普通引用内容。
